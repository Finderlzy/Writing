Java有一个[[../../../08归档/C++/类]]叫做`Scanner`，是专门用来键盘录入的，记住下面几行代码就可以了。

1. 导入`Scanner`
`import java.util.Scanner;`

2. 创建一个`Scanner`对象
`Scanner sc = new Scanner(System.in);`

3. 使用`Scanner`

```java
int i = sc.nextInt();
```

> [!warning] 注意
> 接受数据的变量类型要和“next”后面的类型一致 ← 这个类型的首字母要大写
