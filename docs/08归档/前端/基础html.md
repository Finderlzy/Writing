## 完整页面结构

`<!DOCTYPE html>` → `<html>` → `<head>`（元信息）+ `<body>`（可见内容）

标签要正确闭合，`<body>` 必须在 `<html>` 内部，不能写在 `</html>` 之后

但**组件**（非 `pages/` 下的可复用文件）不需要写完整的 `<html>`结构，只需要写自己的那一小块内容

## `<a href="...">`

`<a>` 提供可点击载体，`href` 指定跳转目标（本站路径、外部网址、锚点 `#xxx`、`mailto:`、`tel:` 等）
