---
aliases:
  - buffer
date: 2026-04-08
---
## 概述

==写入的数据都会先放入缓冲区，等到写满了，一般是换行符出现，才会往电脑输送==。

```
cout << "请输密码：";
// ❌ 没显示？因为还在缓冲区里睡觉！

cout << "请输入密码：" << flush;  // 强制冲走 ✅
// 或 endl（换行+冲走）
```

用cin给某个变量赋值也是，需要用回车冲走

## 优点

减少输送数据的次数，提高效率。