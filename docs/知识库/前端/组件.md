简单来说就是**可以反复利用**的的图案或者元素。
## 组件的导入
在使用组件之前，必须得**import（导入）** 

```
import Social from './Social.astro';
```

## 组件的使用

`<Social/>`
注意末尾有一个`/`

可以把这个理解为一个占位符
## 组件的本质
在组件所在的文件里（上面那个例子中的”Social.astro“）会有关于这个组件的定义以及样式。

使用组件，==本质上是用组件的源代码替换掉新页面的占位符==，就不用手写了

有点像是函数，重点是复用的性质。



[[组件传参]]