## pandding

pandding可以理解为==在文字周围开辟一块空间==，这块空间的大小就是输入的参数，如：

`padding: 0.5rem 1rem;`（上下，左右）。也可以传入4个参数：上、下、左、右

## background-color

background-color是给==文字+pandding==这一整块区域，也就是**整个元素盒子**（文字 + padding 撑出的空白）上色

## color

是==文字==的颜色
