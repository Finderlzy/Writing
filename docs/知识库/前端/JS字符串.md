JS中表示字符串有3种方法——**单引号**、**双引号**、**反引号（模板字符串）**

## 单引号和双引号
引号包围的部分最终会被输出文字。

如果想输出变量需要在引号外面+变量，如：
`"https://www." + platform + ".com/" + username`
需要一些在引号里，一些不在引号里

## 反引号（模板字符串）
多了一个功能，==可以在引号里插入变量==，只需要使用==${} ==，花括号里面放变量即可，如：
\`https://www.${platform}.com/${username} \`
