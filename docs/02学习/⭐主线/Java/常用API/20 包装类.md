---
createdDate: 2026-09-18
---
> 写于 2026-09-18

用一个对象来存储基本数据类型

为什么要这么搞？

1. 所有的类都是`Object`的子类，由于[[02学习/⭐主线/Java/面向对象进阶/多态/多态|多态]]的存在，这意味着函数的形参只要是`Object`就可以接收所有的对象。但是基本数据类型却不可以。因此，使用包装类就可以解决这个问题
2. 有一些数据结构是不能存储基本数据类型的，比如`Set`。使用包装类也可以解决这个问题
